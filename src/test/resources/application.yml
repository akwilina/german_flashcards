spring:
  application:
    name: project_management_app
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
  datasource:
    url: jdbc:h2:mem:gflashcards_test
logging:
  level:
    sql: debug

